# G-LOKOO POS v2.10.9 — Receipt Full-Width / Readable PDF

Receipt printing is adjusted for the current setup where no thermal printer is installed yet.

- Normal paper/PDF mode uses the available page width.
- Receipt text, logo, shop name, totals and item details are larger.
- Thermal 58mm/80mm mode remains available for later when a thermal printer is configured.
- No database migration is required.

Keep the existing `.env.local` file.
